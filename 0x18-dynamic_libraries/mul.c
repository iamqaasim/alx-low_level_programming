#include "main.h"

/**
 * mul - multiply two numbers from input
 * @a: first aparamet
 * @b: second parameter
 *
 * Description: multiply two numbers
 * Return: Always (0).
 */

int mul(int a, int b)
{
	return (a * b);
}
