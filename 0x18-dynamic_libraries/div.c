#include "main.h"

/**
 * div - divides two numbers from input
 * @a: first aparamet
 * @b: second parameter
 *
 * Description: divides two numbers
 * Return: Always (0).
 */

int div(int a, int b)
{
	return (a / b);
}
