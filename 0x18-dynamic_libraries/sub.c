#include "main.h"

/**
 * sub - subtract two numbers from input
 * @a: first aparamet
 * @b: second parameter
 *
 * Description: subtract two numbers
 * Return: Always (0).
 */

int sub(int a, int b)
{
	return (a - b);
}
