#include "main.h"

/**
 * mod - modulus of two numbers from input
 * @a: first aparamet
 * @b: second parameter
 *
 * Description: modulus of two numbers
 * Return: Always (0).
 */

int mod(int a, int b)
{
	return (a % b);
}
