#include <stdio.h>

/*
 * main prints lower case alphabet
 *
 * return 0 for success
 */

int main (void)
{
	char ch = 'a';

	while(c <= 'z')
	{
		putchar(ch);
		c++;
	}
	putchar('\n');
	return (0);
}
