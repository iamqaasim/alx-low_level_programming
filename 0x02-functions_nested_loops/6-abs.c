#include "main.h"

/**
 * _abs() - computes absolute value
 *
 * Return: 0 for success
 */

int _abs(int)
}
	int value = int;
	
	if (value < 0)
	{
		value = value * -1
		
		_putchar(value);
	}
	else
	{
		_putchar(value);
	}
	return (0);
}
