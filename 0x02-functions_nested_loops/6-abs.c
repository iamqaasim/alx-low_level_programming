#include "main.h"

/**
 * _abs() - computes absolute value
 *
 * Return: 0 for success
 */

int _abs(int)
}
	if (int < 0)
	{
		int value = int * -1
		
		_putchar(value);
	}
	else
	{
		_putchar(int);
	}
	return (0);
}
